{% extends 'base.html.twig' %}

{% block title %}Reservation index
{% endblock %}

{% block body %}
	<h1>Reservation index</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Number_of_ticket</th>
				<th>Firstname</th>
				<th>Lastname</th>
				<th>Ticket</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for reservation in reservations %}
				<tr>
					<td>{{ reservation.reservation.id }}</td>
					<td>{{ reservation.reservation.numberOfTicket }}</td>
					<td>{{ reservation.reservation.firstname }}</td>
					<td>{{ reservation.reservation.lastname }}</td>
					<td>{{ reservation.reservation.ticket }}</td>
					<td>
						<a href="{{ path('app_reservation_show', {'id': reservation.reservation.id}) }}">show</a>
						<a href="{{ path('app_reservation_edit', {'id': reservation.reservation.id}) }}">edit</a>
						<a href="{{ path('app_reservation_delete', {'id': reservation.reservation.id}) }}">supprimer</a>
					</td>
				</tr>

			{% else %}
				<tr>
					<td colspan="7">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
	<a href="{{ path('app_payment_new', {'reservations': reservations}) }}">Payer</a>
	<a href="{{ path('app_reservation_new') }}">Create new</a>
{% endblock %}
